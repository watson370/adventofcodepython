"""take in an array of ints, return an array of ints"""
def round(vals, index):
    index += 1
    opcode = getOpCode(vals[index])
    # create a dictionary, keep in a class so it doesn't keep getting created
    # key will be the opcode, value will be a tuple with name, and argument length

def getOpCode(val):
    return val
